spring:
  application:
    name: fileview-service
  profiles:
    active: local

application:
  description: 文件服务

hystrix:
 metrics:
  enabled: true
 command:
  default:
   execution:
    isolation:
     thread:
       timeoutInMilliseconds: 12000
server:
  servlet:
    context-path: "/"